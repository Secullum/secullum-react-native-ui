---
name: RangeDatePicker
route: /range-date-picker
menu: Components
---

import { Playground, PropsTable } from 'docz';
import { View } from 'react-native';
import { RangeDatePicker } from '../../src/components/RangeDatePicker.web.tsx';

# RangeDatePicker

## Properties

<PropsTable of={RangeDatePicker} />

## Example

<Playground>
  <View style={{ height: 350 }}>
    <RangeDatePicker
      label="label"
      startDate={new Date(1990, 9, 1)}
      endDate={new Date(1990, 9, 31)}
      onStartDateChange={startDate => alert(startDate)}
      onEndDateChange={endDate => alert(endDate)}
    />
  </View>
</Playground>
