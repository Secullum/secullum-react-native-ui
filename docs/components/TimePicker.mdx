---
name: TimePicker
route: /time-picker
menu: Components
---

import { Playground, PropsTable } from 'docz';
import { TimePicker } from '../../src/components/TimePicker.web.tsx';

# TimePicker

## Properties

<PropsTable of={TimePicker} />

## Example

<Playground>
  {() => {
    class TimePickerExample extends React.Component {
      constructor() {
        this.state = {
          value: '08:00',
          endDate: new Date(1990, 9, 31)
        };
      }

      render() {
        return (
          <TimePicker
            label="Time"
            value={this.state.value}
            onChange={value => this.setState({ value })}
          />
        );
      }
    }

    return <TimePickerExample />;
  }}
</Playground>