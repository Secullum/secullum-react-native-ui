---
name: Question
route: /question
menu: Components
---

import { Playground, PropsTable } from 'docz';
import { Question } from '../../src/components/Question.tsx';

# Question

## Properties

<PropsTable of={Question} />

## Example

<Playground>
  {() => {
    class QuestionExample extends React.Component {
      constructor() {
        this.state = {
          messageVisible: true
        };
      }

      render() {
        return (
          <Question
            message='This is a message'
            type='question'
            visible={this.state.messageVisible}
            okButton={{
              text: 'OK',
              primary: true,
              onPress: ()=> {
                alert('OK Button Pressed');
              }
            }}
            cancelButton={{
              text: 'Cancel',
              primary: false,
              onPress: ()=> {
                this.setState({messageVisible: false});
              }
            }}
          />
        );
      }
    }

    return <QuestionExample />;

}}

</Playground>
