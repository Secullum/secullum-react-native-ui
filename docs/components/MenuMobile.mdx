---
name: MenuMobile
route: /menu-mobile
menu: Components
---

import { Playground, PropsTable } from 'docz';
import { Text, View } from 'react-native';
import { MenuMobile } from '../../src/components/MenuMobile.tsx';

# MenuMobile

## Properties

<PropsTable of={MenuMobile} />

## Example

<Playground>
  <View style={{ height: 600, width: 400, borderWidth: 1, overflow: 'hidden' }}>
    <MenuMobile
      ref={menu => menu && menu.open()}
      renderLogo={() => (
        <View>
          <Text style={{ fontFamily: 'Roboto', fontSize: 40 }}>My App</Text>
        </View>
      )}
      menu={[
        { text: 'Home', path: 'Home', textStyle: { color: 'red' } },
        {
          text: 'Settings',
          path: 'Settings',
          textStyle: { color: 'green' },
          submenu: [
            { text: 'Basic Settings', path: 'BasicSettings', textStyle: { color: 'blue' } },
            { text: 'Advanced Settings', path: 'AdvancedSettings', subText: 'soon' }
          ]
        },
        { text: 'About', path: 'About' }
      ]}
      onMenuPress={path => alert(path)}
    />
  </View>
</Playground>
